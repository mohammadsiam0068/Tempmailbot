{
  "name": "tempmail-bot",
  "version": "1.0.0",
  "description": "Telegram Bot for Cloudflare Workers",
  "main": "api/webhook.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
